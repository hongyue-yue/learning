git --version                                 git版本说明
git config --global user.name "<用户名>"      git用户名设定
git config --global user.email "<电子邮件>"   git用户邮箱设定
git config --global color.ui auto             git彩色显示
mkdir tutorial
cd tutorial
git init           将新创建的tutorial目录设置到git数据库
git status         查看工作树和索引的状态
git add <file>     在<file>指定加入索引的文件。用空格分割可以指定多个文件
git commit -m ""   将已经加入索引的文件进行提交
git log            可以在数据库的提交记录看到新的提交
git remote add <name> <url>  请使用remote指令添加远程数据库。在<name>处输入远程数据库名称，在<url>处指定远程数据库的URL。
git push <repository> <refspec>       使用push命令向数据库推送更改内容。<repository>处输入目标地址，<refspec>处指定推送的分支。
git clone <repository> <directory>    使用clone指令可以复制数据库，在<repository>指定远程数据库的URL，在<directory>指定新目录的名称。
git pull <repository> <refspec>       将远程数据库内容下拉到本地数据库
